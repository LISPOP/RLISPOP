---
title: "Data Visualization"
format: html
editor: source
toc: true
toc-location: left
execute:
  warning: false
  message: false
  echo: true

---

```{r setup, include=FALSE, echo=F}
knitr::opts_chunk$set(echo = TRUE, warning=F, message=F)

```


```{r define-output-lines, echo=F}
library(knitr)
hook_output <- knit_hooks$get("output")
knit_hooks$set(output = function(x, options) {
   lines <- options$output.lines
   if (is.null(lines)) {
     return(hook_output(x, options))  # pass to default hook
   }
   x <- unlist(strsplit(x, "\n"))
   more <- "..."
   if (length(lines)==1) {        # first n lines
     if (length(x) > lines) {
       # truncate the output, but add ....
       x <- c(head(x, lines), more)
     }
   } else {
     x <- c(if (abs(lines[1])>1) more else NULL, 
            x[lines], 
            if (length(x)>lines[abs(length(lines))]) more else NULL
           )
   }
   # paste these lines together
   x <- paste(c(x, ""), collapse = "\n")
   hook_output(x, options)
 })
```
# Introduction

## Load libraries

```{r load-libraries}
library(tidyverse)
library(here)
library(haven)
```

## Data Import

```{r load-data}

shs<-read_sav(file=here("../course_data/shs.sav"))

shs %>% 
  #Select income, taxes, dwelling type and housing tenure
  select(Income=HH_TotInc, Taxes=TX001,Dwelling=DwelTyp, Tenure=Tenure, Province=Prov)->shs_subset
#glimpse
glimpse(shs_subset)


```


```{r}
#| label: asfactor
shs_subset<-as_factor(shs_subset)
#Compare now
glimpse(shs_subset)
```

# The syntax of ggplot()

The basic syntax of ggplot2 is universal throughtout the whole library. It looks like this:

`ggplot(data=name_of_data_frame, mapping=aes(x=name_of_x_variable, y=name_of_y_variable))+geom_*`

So what are we looking at here: 
1. The first part is the call to `ggplot()`. It requires a `data` argument, which must be a data frame. If you pipe a dataset to it, you an replace the argument with a . So hypothetically, these are equivalent.

```{r ggpplot1, eval=F}
ggplot(data=dat, aes(x=x, y=y))
dat %>% 
  ggplot(., data=., aes(x=x, y=y))
```
2. Inside the `ggplot()` call, it is necessary to define the `mapping` of the plot. This specifies which variable in the data frame is mapped to which feature of the graph. In the simplest form of a one-variable graph, like a histogram,only an x- is specified.
Note, that it is possible to only specify a y- variable, for the case of a bar graph that is set horizontally.

In theory, `aes()` is set to a `mapping` argument, i.e. 

```{r ggplot2, eval=F}
dat %>% 
  ggplot(., mapping=aes(x=x, y=y))
```

However, because R matches arguments by position as well, as long as you put the mapping *after* the data argument, you don't need to worry about typing the `mapping=`.  I *never* do this anymore. So this is fine:

```{r ggplot3, eval=F}
dat %>% 
  ggplot(., aes(x=x, y=y))
```

Here, isalso where we also specify grouping (third or fourth or figth variables) mapped to the aesthetic features of a graph, such as thickness, color, fill, point shape, point size, transparency, etc. 

3. Lastly, it is necessary to add a `geom` to the graph, connected to the main call with a `+`. Note that the `+` functions sort of like the `%>%` character in the rest of the `tidyverse`. The difference is explained by the fact that `ggplot` was developed *before* the `tidyverse`.

This effectively specifies the way to represent the mappings in a geometric feature (e.g. point, shape, line, bar).

A full list of `geoms` is available here: [](https://ggplot2.tidyverse.org/reference/). 

Note they range from the standard: `geom_line()`, `geom_point()`, `geom_bar()` to the more surprising, e.g. `geom_text()` for text labels, `geom_abline()` for a straight vertical or horizontal line. 

```{r ggplot4,eval=F}
dat %>% 
  ggplot(., aes(x=x, y=y))+geom_point()
```


# One Variable


Sometimes we want to visualize the *distribution* of *one* variable. 


## Histograms

A histogram is a classic and important graph design to visualize the distribution of values for a *numeric* variable. It can answer a lot of different questions such as: 

- are values of a variable clustered around an average, or are they spread out quite a bit?
- Are there any outlier instances of a variable that are worth looking at? 

```{r histogram1}

ggplot(shs_subset, aes(x=Income))+geom_histogram()
```

We can also run the same code in some of the classic `tidyverse` syntax where we pipe data right to `ggplot()`
Sometimes I do this, and sometimes I don't; it really depends. 

```{r histogram2}
shs_subset%>% 
ggplot(., aes(x=Income))+geom_histogram()
```

We can also save plots in objects and then modify them later. 

```{r histogram3}
#Save in histogram1
shs_subset%>% 
ggplot(., aes(x=Income))+geom_histogram()->histogram1
#Now print. 
histogram1
```

We'll see how we can use this feature in the next section. 

## Density Plots

Another example of a type of plot that shows the distribution of a variable is a `density` plot. This is a *smoothed* version of a histogram. In these graphs, the y-axis value is *not* the number of instances of a variable in a bin, but the probability that a variable takes on the value at that y-axis or less. 

```{r density-plot}
shs_subset%>% 
ggplot(., aes(x=Income))+geom_density()
```

::: {important.callout}
Notice right away something useful about `ggplot`. We used the exactly same syntax to build the logic of the plot and then just changed the `geom` from a histogram to a density plot. 

:::

## Bar graphs 

Note that the histogram and the density plot only work with. You may have a categorical variable that you wish to show. Remember categorical variables have categories as values. In this instances we have the variable `School Type` which gives us a the value of whether or not a board is Catholic or Public. What if we would like to show the distribution of these *categories*. 

```{r bar1, output.lines=10}
#Check the names
names(shs_subset)
#Print the values of Board Type just to check
shs_subset$Dwelling
```

Note a very important feature here: we do *not* have the actual counts of public and catholic schools. In this instance, we can pass `Dwelling` right to `ggplot()` and it will count the groups for us. 

```{r bar2}
ggplot(shs_subset ,aes(x=Dwelling))+geom_bar()
```

In many other instances, we *will* have the counts. In this instance, we use a different function for making a bar graph, namely the `geom_col()` function.

```{r bar3}
shs_subset %>% 
  group_by(Dwelling) %>% 
  summarize(count=n())

```


Now that we have the groups that show the counts, we can make a bar graph. Note the differences below. First, although the `x-axis` variable is the same i.e. `School Type`, but note that we also have a variable that contains the counts. So we actually need to specify a `y variable`. And we use the variable name `count` which we set in the `summarize` function. 

```{r bar4}
shs_subset %>% 
  group_by(Dwelling) %>% 
  summarize(count=n()) %>% 
  ggplot(., aes(x=Dwelling, y=count))+geom_col()


```

::: {tip.callout}
`geom_bar()` is for when you have no counts to specify the height of the bar graph's bars. `geom_col()` is when you *do* have a column that can specify the heights of the bars.

:::


#### Bar graphs of percents

Notice above that we had *counts* of in certain boards, but what if we want to have *percent* of schools in certain boards. This is an extremely common problem so it is worth focussing how to get it right here.

Recall this code gives us the number of schools in each type of board. 

```{r count-schools-by-board}
shs_subset %>% 
  group_by(Dwelling) %>% 
  summarize(count=n())
```


What we need is a column to the right of the `count` column that is the percentage each row makes up of the total.  The first go was a `summarize` function, but now we need to `mutate` those values to be percentages.

```{r calculate-percentages}
shs_subset %>% 
  group_by(Dwelling) %>% 
  summarize(count=n()) %>% 
  mutate(percent=count/sum(count))
```

Now we have the percentages! So roughly 33\% of schools in Ontario are in Catholic school boards. So what if we want to graph those.

Because we have the y-value we want to specify, we will use `geom_col()`. We will come to modifying the scales to report percentages later. 

```{r bar-percent}
shs_subset %>% 
  group_by(Dwelling) %>% 
  summarize(count=n()) %>% 
  mutate(percent=count/sum(count)) %>% 
  ggplot(., aes(x=Dwelling, y=percent))+geom_col()

```


## Boxplots

Another type of graph that is used to visualize a variable is the `boxplot`. This contains a lot of statistical information.
1. The thick line in the middle is the `median` value`. So half the values are above that line and half are below. 
2. The outer thin lines are the `quartiles`. 25\% of the values lie on either side of the thick line. Combined, the two thin lines (e.g. the box are known as the `IQR` or the `interquartile rangew`.)
3. The thin lines from the box to the outer edges are values that stretch from the 25\% (75\%) percentile to the `minimum` or the `maximum`.
4. The dots are known as `outliers`, data points that are far away from the median.



```{r boxplot1}
ggplot(shs_subset, aes(x=Income))+geom_boxplot()
```

# Two and More Variables

Sometimes, however, we want to visualize relationships between *two* variables. Most of the graphs above can be easily extended to add a second variable. 

### y - Variables

For example, what if we want to look at the distribution of school math results by `board`. note that each individual value of grade 9 math is from a school; but schools are grouped into school boards. Some school boards are wealthier, some are poorer, some are rural, some are urban , some are Catholic, some are public. So we might want to look at the distribution of values by school board. 

Let's put Province on the x-axis and the `Income` on the y-axis


```{r boxplot3}
  ggplot(shs_subset, aes(x=Province, y=Income))+geom_boxplot()
```


One nice feature of `gggplot2` is that we can just quickly switch the x and y variables. 

::: {tip.callout}
Switching the x- and y- variables is often really useful with factor variables with long names; they can be more legible horizontally!
:::

```{r boxplot4, fig.height=12, fig.width=8}
ggplot(shs_subset, aes(y=Province, x=Income))+geom_boxplot()
```


### Grouping, Summarizing and Graphing

Rather than a boxplot that shows the median and the quintiles automatically, we might just want the average income. To do this we need to calculate the mean by grouping and summarizing and then passing it to `geom_col()`..

```{r}
#| label: group-mean

shs_subset %>% 
  group_by(Province) %>% 
  summarize(Average_Income=mean(Income))->average_incomes
#print
average_incomes
```

Notice how I saved the incomes in an object there, I just did that to make it work a bit easier. 
Remember: We have the actual numbers needed to specify the height of the bars, so we use `geom_col()`!

```{r}
#| label: plot-average-incomes

average_incomes %>% 
  ggplot(., aes(x=Province, y=Average_Income))+geom_col()
```
### Scatterplots

Scatterplots are the most common way to visualize two numeric variables. 

Here we can examine the correlation between the percent of students meeting the provincial standard for grade 9 math and the percent of students meeting the standard for literacy.

```{r scatterplot1}
shs_subset %>% 
  ggplot(., aes(x=Income, y=Taxes))+geom_point()
```

The relationship between the two variables can be spelled out with a `geom_smooth()` which prints a line of best fit. The most basic way is to fit a straight line between the two variables. 

```{r scatterplot2}
shs_subset %>% 
  ggplot(., aes(x=Income, y=Taxes))+geom_point()+geom_smooth(method="lm")
```

Another method is to fit something that is more curvy; a `loess` line only draws the line using a few points at a time, so it allows for curves.


```{r scatterplot3}
shs_subset %>% 
  ggplot(., aes(x=Income, y=Taxes))+geom_point()+geom_smooth(method="loess")
```

### Aesthetics

But in addition to adding a y-variable to a lot of the graphs above we can also vary the aesthetics of a graph with one variable to show a different variable. For instance, if we go back to a density plot of Income , we can show the difference by type of dwelling of residence by specifying `fill=Dwelling`

```{r fill}
ggplot(shs_subset, aes(x=Income, fill=Dwelling))+geom_density()
```

Note that every geom has different aesthetics can be modified. For example, histograms and bar graphs and density plots can all have their fill or their color modified. 

The best way to read what can be modified is to look at the help file for each `geom`. 

```{r help-file, eval=F}
?geom_density()
```

```{r img1, echo=F}

include_graphics(path=here("Images/help_file.png"))
```
So here, we see that we can modify a density plot's `x`, `y`, `alpha`, `color`, `fill`, `gtroup`, `linetype`, `size` and `weight`. Play around with the different aesthetics. 


```{r col}
ggplot(shs_subset, aes(x=Income, col=Dwelling))+geom_density()
```

Note also that you can combine aesthetics to vary two things at once. So we can modify the `fill` and the `alpha` at the same time. Now you get not only a different color, but a different transparency as well. 


```{r alpha}
ggplot(shs_subset, aes(x=Income, fill=Dwelling, alpha=Dwelling))+geom_density()


```

*Notice that when you map aesthetics to a variable's values the legend is automatically updated! *

We can do this with the histogram: 

```{r boxplot5}
ggplot(shs_subset, aes(x=Income, fill=Dwelling, alpha=Dwelling))+geom_histogram()
```

We can also do this with bar graphs. And there are a few ways to do this. The easiest way is just to show the counts of values in each group. 

Here we show the number of schools in each board type and language group:

```{r bar5}

ggplot(shs_subset, aes(x=Dwelling, fill=Tenure))+geom_bar()
```

However, this style of graph can be misleading, the eye is drawn to the top of the bar making it hard to grasp what proportion of the schools is in the two different groups. So we can put the bars side-by-side, like this:

```{r bar6}
ggplot(shs_subset, aes(x=Dwelling, fill=Tenure))+geom_bar(position="dodge")
```

Sometimes we want to just show the pure distribution within groups:

```{r bar7}
ggplot(shs_subset, aes(x=Dwelling, fill=Tenure))+geom_bar(position="fill")

```

note though that this shows something very specific. It shows the percentage of Dwelling types that are owned with or without a mortgage or rented. 

### Facets

Another way that we can add another variable is with panels or `facets`. 

There are two types of facets: `facet_wrap()` and `facet_grid()`

This is a `wrap`
```{r facets1}

ggplot(shs_subset, aes(x=Income))+geom_histogram()+facet_wrap(vars(Dwelling))
```

And this is a `grid`

```{r facets2}
ggplot(shs_subset, aes(x=Income))+geom_histogram()+facet_grid(vars(Dwelling))

```

There are different things you can do with both. With `facet_wrap` you can specfiy how many rows and columns you want. 
```{r facets3}
ggplot(shs_subset, aes(x=Income))+geom_histogram()+facet_wrap(vars(Dwelling), nrow=1)
```

```{r facets4}
ggplot(shs_subset, aes(x=Income))+geom_histogram()+facet_wrap(vars(Dwelling), nrow=2)
```


And in `facet_grid` you can specify a row and a column variable. 

```{r facets6}

ggplot(shs_subset, aes(x=Income))+geom_histogram()+facet_grid(rows=vars(Dwelling), cols=vars(`Tenure`), scales='free')

```



#### Combining a pivot with facets

Quite properly, a lot of our data are stored in separate columns. That is to say, the income and taxes are two separate variables. 

However, if we wanted to show multiple variables in panels, then, in keeping with the grammar of `ggplot` we need to turn that wide data into long data. 

Suppose we want to show variable in one graph, one facet per variable. 

We are going to need to pivot down both income and taxes into *one variable*. 

```{r pivot1}
names(shs_subset)
```

```{r pivot2}
shs_subset %>% 
  pivot_longer(., cols=c(Income, Taxes), names_to=c('Measure'), values_to=c("Dollars")) %>% 
  glimpse()
```


Now we can do a density plot *for each individual variable* using the same grammar and syntax as the density plot above!!!!

```{r pivot3}
shs_subset %>% 
  pivot_longer(., cols=c(Income, Taxes), names_to=c('Measure'), values_to=c("Dollars"))%>% 
ggplot(., aes(x=Dollars))+geom_histogram()+facet_wrap(vars(Measure))
```

And we can add another grouping variable:

```{r pivot4}
shs_subset %>% 
  pivot_longer(., cols=c(Income, Taxes), names_to=c('Measure'), values_to=c("Dollars")) %>% 
ggplot(., aes(x=Dollars))+geom_histogram()+facet_wrap(vars(Measure, Province), nrow=4)
```
# Specialized Graphs

There are some specialized graphs that are worth taking into account: 

## Ridgeline or Joy Plots

These basically density plots of a numeric variable by groupings of a categorical variable. They were first made famous for being the cover of Joy Division's famous album Unknown Pleasures. 

```{r joy-plot, echo=F}
include_graphics(path=here("Images/joy_division.jpeg"))
```

To create this, we need to load the library `ggridges`
```{r ridgelines}
#install.packages('ggridges')
library(ggridges)
shs_subset %>% 
  ggplot(., aes(x=Income, y=Province, fill=Dwelling ))+geom_density_ridges()

```

<!-- ## Cleveland Plots -->

<!-- Cleveland plots look like this:  -->
<!-- ```{r cleveland1, echo=F} -->
<!-- include_graphics(here("Images/cleveland_plot.png")) -->
<!-- ``` -->

<!-- It's really easy. It's basically just a bar graph but with `geom_point()` instead of `geom_bar()` -->

<!-- Suppose we want to show the average number of students meeting the provincial standards for grade 9 math by school board. -->

<!-- ```{r cleveland2} -->
<!-- shs_subset %>%  -->
<!--   group_by(Province) %>%  -->
<!--   summarize(Average=mean(Income, na.rm=T))  -->

<!-- ``` -->

<!-- ```{r cleveland4} -->
<!-- shs_subset %>%  -->
<!--   group_by(Province) %>%  -->
<!--   summarize(Average=mean(Income, na.rm=T)) %>%  -->
<!--   ggplot(., aes(y=Province, x=Average))+geom_point() -->
<!-- ``` -->

<!-- And we can connect them with lines, if we want, through `geom_segment`. For this we need to specify where the line should end on the y-axis and on the x-axis. -->

<!-- ```{r cleveland4a} -->
<!-- shs_subset %>%  -->
<!--   group_by(Province) %>%  -->
<!--   summarize(Average=mean(Income, na.rm=T)) %>%  -->
<!--   ggplot(., aes(y=Province, x=Average))+geom_point()+geom_segment(aes(yend=Province), xend=0) -->
<!-- ``` -->

<!-- They are desired because they are useful in showing changes in values by categories.  -->

<!-- Notice that we actually have the change in `shs_subset`. So, we could actually combine the percent of students meeting the standards with the corresponding `delta` variable and we would have the percent of students meeting the standard from three years ago, right? -->

<!-- ```{r cleveland5} -->
<!-- #Start with the data frame -->
<!--   shs_subset %>%  -->
<!--   #Form groups for averages by board -->
<!--   group_by(Province) %>%  -->
<!--   #Get 2020 average and average change from 2017 -->
<!--   summarize(`2020`=mean(Income, na.rm=T), delta=mean(delta_grade_9_academic_math, na.rm=T)) %>%  -->
<!--   #Subtract the delta from the 2020 average to get the 2017 -->
<!--     mutate(`2017`=`2020`-delta) %>%  -->
<!--   #Pivot 2020 and 2017 down -->
<!--   #Name one column Year and one Average -->
<!--     pivot_longer(cols=c(`2020`, `2017`), names_to=c("Year"), values_to=c("Average")) %>%  -->
<!--   #Plot x= Average, y=Board Name -->
<!--   ggplot(., aes(x=Average, y=Province))+ -->
<!--   #Distinguish points by year with color -->
<!--   geom_point(aes(col=Year))+ -->
<!--   #Add the lines -->
<!--   geom_line(aes(group=Province)) -->
<!-- ``` -->

<!-- One really cool thing we could do is to order the school boards by the size of the delta. -->
<!-- ```{r cleveland6, fig.height=12, fig.width=8} -->
<!-- #Start with the data frame -->
<!--   shs_subset %>%  -->
<!--   #Form groups for averages by board -->
<!--   group_by(Province) %>%  -->
<!--   #Get 2020 average and average change from 2017 -->
<!--   summarize(`2020`=mean(Income, na.rm=T), delta=mean(delta_grade_9_academic_math, na.rm=T)) %>%  -->
<!--   #Subtract the delta from the 2020 average to get the 2017 -->
<!--     mutate(`2017`=`2020`-delta) %>%  -->
<!--   #Pivot 2020 and 2017 down -->
<!--   #Name one column Year and one Average -->
<!--     pivot_longer(cols=c(`2020`, `2017`), names_to=c("Year"), values_to=c("Average")) %>%  -->
<!--   #Plot x= Average, y=Board Name -->
<!--   ggplot(., aes(x=Average, y=fct_reorder(Province, delta, .desc=T)))+ -->
<!--   #Distinguish points by year with color -->
<!--   geom_point(aes(col=Year))+ -->
<!--   #Add the lines -->
<!--   geom_line(aes(group=Province)) -->
<!-- ``` -->

# Advanced Customization

## Reducing Categories and Changing Text
A lot of times that we want to change the text on category labels. We might want to do this to eliminate some fringe categories or we might want to make labels more readable.  

Let's work with that graph that shows the average incomes by province. 

```{r labels1}
average_incomes %>% 
  ggplot(., aes(x=Province, y=Average_Income))+geom_col()
```

We can rotate it to make the labels more readable:
```{r labels2}
average_incomes %>% 
  ggplot(., aes(y=Province, x=Average_Income))+geom_col()
```

That goes a long way, but honestly, there are some things we can do to clean this up. 

Let's do a few things:

1. Filter out the territorial capitals and change `Atlantic Provinces` to just `Atlantic`. 
2. Change the names of the major categories to just `Catholic` and `Public`.

Remember, we are not graphing the `shs_subset` at this moment, we are graphing the `average_incomes` table that we made. So that is where we will recode the province variable to shorten the variable name. 
```{r recode}
library(car)
average_incomes %>% 
  mutate(Province=Recode(Province, "'Atlantic provinces'='Atlantic'"))->average_incomes
```
Now we can check again. It is changed in`average_incomes` but not in `shs_subset`.

```{r check2}
table(average_incomes$Province)
table(shs_subset$Province)
```
```{r labels3}
average_incomes %>% 
  filter(Province!="Territorial capitals") %>% 
  ggplot(., aes(y=Province, x=Average_Income))+geom_col()

```

## Mapping Versus Setting 

We often want to modify the colors of graphs for a lot of reasons, sometimes it is as simple as complying with a media company's color palette. For example, the Toronto Star is a heavily blue branded outlet; while the Globe and Mail heavily emphasizes red. 

There are key things to note when changing visual aesthetics of a graph: there is a *mapping* and a *setting*. 

In the following graph, the color of the points is just *set* to black.
```{r mapping1}
shs_subset %>% 
ggplot(., aes(x=Income, y=Taxes))+geom_point()
```

We might want to *set* the colors of these points to be something, or we might want to set the size of the points, just to better fit the medium in which we are publishing. 

We can do this straightforwardly inside the `geom_point()`

```{r mapping2}
shs_subset %>% 
ggplot(., aes(x=Income, y=Taxes))+geom_point(color="red")
```

The very best list of R colors available for use is [here](http://www.stat.columbia.edu/~tzheng/files/Rcolor.pdf). 

We might also want to set the size, or the shape of the points. 

```{r mapping3}
shs_subset %>% 
ggplot(., aes(x=Income, y=Taxes))+geom_point(color="red", size=5, shape=3)
```

You can find the list of point shapes available [here](http://www.sthda.com/english/wiki/ggplot2-point-shapes). 

We can also set the `fill` of some point shapes, but not all. There is no substitute for playing around. 

```{r mapping4}
shs_subset %>% 
ggplot(., aes(x=Income, y=Taxes))+geom_point(color="red", fill="blue", shape=23)
```

However, changing the appearance of visual aesthetics which have been mapped to a category is a different process. Here, the colors are not *set*, they are *mapped* to the categories dwelling type. 

```{r mapping5}
shs_subset %>% 
ggplot(., aes(x=Income, y=Taxes, col=Dwelling))+geom_point()
```

Suppose we want to use a different set of colors to be used for the mapping, like, let's say, red and green.

To do so, we need to introduce a series of functions that have the same type of structure. These are: 

`scale_col_discrete()`
`scale_col_continuous()`
`scale_fill_discrete()`
`scale_fill_continuous`
`scale_size_discrete()`
`scale_size_continuous`

In order to make this work, we need to know what type of aesthetic we need to modify (e.g. the color, the fill, the size, the shape) and what type of variable is the mapping variable. 

In this case, we have mapped the `colors` to a `discrete` variable, which is another name for categorical variables. So we use `scale_color_discrete`

```{r mapping6}
shs_subset %>% 
ggplot(., aes(x=Income, y=Taxes, col=Dwelling))+geom_point()+scale_color_discrete(type=c("darkgreen", "gold", "lightgreen"))

```

Note that you can also reverse the colors to specifically link a color to level
```{r mapping7}
shs_subset %>% 
ggplot(., aes(x=Income, y=Taxes, col=Dwelling))+geom_point()+geom_point()+scale_color_discrete(type=c("lightgreen", "gold" , "darkgreen"))

```

Some scales can be mapped to numeric or continuous variables, while other can only be mapped to continuous variables. 


### Picking Palettes
There are also a series of R packges that let us pick pre-built palettes of colors. One of the best is the [`RColorbrewer`](https://www.r-graph-gallery.com/38-rcolorbrewers-palettes.html) package

When we are mapping a color palette to a categorical variable, like we are here with `Dwelling`,  and we provide the `type` argument (either `seq`, `qualitative`, or `diverging`) and then pick the palette. 

```{r palettes1}
#install if necessary
#install.packages('RColorBrewer')
library(RColorBrewer)
shs_subset %>% 
ggplot(., aes(x=Income, y=Taxes, col=Dwelling))+geom_point()+scale_color_brewer(type="seq", palette="Greens")


```

<!-- But we can also map the color to a continuous (numeric) variable using the Brewer palettes using the `scale_color_distill()` -->


<!-- ```{r palettes2} -->

<!-- shs_subset %>%  -->
<!-- ggplot(., aes(x=Income, y=Taxes, col=Dwelling))+geom_point()+geom_point()+scale_color_distiller(type="seq", palette="Reds") -->


<!-- ``` -->




<!-- ## Text -->

<!-- Often we want to add text to graphs in order to make it clear which values the graph is representing. To do this we use the `geom_text()` function. It basically represents another layer in the plot.  -->

<!-- Let's make the percent plot of schools again: -->

<!-- Here are the schools by school board data again. -->

<!-- ```{r text1} -->
<!-- shs_subset %>%  -->
<!--   filter(!is.na(Board)) %>%  -->
<!--   group_by(Board) %>%  -->
<!--   summarize(count=n()) %>%  -->
<!--   ggplot(., aes(x=Board, y=count))+geom_col()+geom_text(aes(label=count), vjust=-0.5) -->

<!-- ``` -->

<!-- ```{r text2} -->
<!-- shs_subset %>%  -->
<!--   filter(!is.na(Board)) %>%  -->
<!-- group_by(Board, `School Language`) %>%  -->
<!--   summarize(count=n()) %>%  -->
<!--   ungroup() %>%  -->
<!--   mutate(percent=count/sum(count)) %>%  -->
<!--   ggplot(., aes(x=Board, y=percent, fill=`School Language`, group=`School Language`))+geom_col(position="dodge")+geom_text(aes(label=round(percent*100, 2)), position=position_dodge(0.9))  -->


<!-- ``` -->


## Factor Orders

Often we want to change the order of categories for categorical variables in order to communicate some information. Here is the graph that shows the average income by province. 


```{r factor1}
average_incomes %>% 
ggplot(., aes(y=Province, x=Average_Income))+geom_col()
```


It would be nice to order the names of the provinces so that the ones with the highest incomes are at the top. Here we use a powerful tidyverse package called `forcats`. It provides the `fct_relevel()` function which changes the order of the levels of its main argument by the values in its second argument. 


```{r factor3}
average_incomes %>% 
  #fct_reorder reorders the levels ofthe factor variable by the values of the second numeric variable
  ggplot(., aes(y=fct_reorder(Province, Average_Income), x=Average_Income))+geom_col()


```

We can reverse the orders like this, by adding `.desc=T`.

```{r factor4}
average_incomes %>% 
  #fct_reorder reorders the levels ofthe factor variable by the values of the second numeric variable
  ggplot(., aes(y=fct_reorder(Province, Average_Income), x=Average_Income, .desc=T))+geom_col()
```



## Axis and Graph Labels

Notice now however that the y-axis labels are kind of ....not very nice. The `labs()` layer is very easy to modify.

```{r axis-labels1}
average_incomes %>% 
  #fct_reorder reorders the levels ofthe factor variable by the values of the second numeric variable
  ggplot(., aes(y=fct_reorder(Province, Average_Income), x=Average_Income))+geom_col()+labs(x="Average Household Income", y="Province", title="Average Total Household Income By Province")
```

We can also wrap a long title onto two lines using the `str_wrap()` function which comes from the `stringr` library

```{r axis-labels2}
average_incomes %>% 
  #fct_reorder reorders the levels ofthe factor variable by the values of the second numeric variable
  ggplot(., aes(y=fct_reorder(Province, Average_Income), x=Average_Income))+geom_col()+labs(x="Average Household Income", y="Province", title=str_wrap("Average Total Household Income By Province", 30))
```
## Themes

The overall look of the graph, for lack of a better word is controlled by the `theme` function. 

There are a series of built-in themes you can set to the graph to change the look. 

There are also additional themes stored in the `ggtheme` package.

For example, this is the `theme_grey()`. It automatically turns everything to shades of grey.

```{r theme1}

```

`Theme_bw()`
```{r theme2}
average_incomes %>% 
  #fct_reorder reorders the levels ofthe factor variable by the values of the second numeric variable
  ggplot(., aes(y=fct_reorder(Province, Average_Income), x=Average_Income))+geom_col()+labs(x="Average Household Income", y="Province", title="Average Total Household Income By Province")+theme_bw()
```

```{r theme3}
average_incomes %>% 
  #fct_reorder reorders the levels ofthe factor variable by the values of the second numeric variable
  ggplot(., aes(y=fct_reorder(Province, Average_Income), x=Average_Income))+geom_col()+labs(x="Average Household Income", y="Province", title="Average Total Household Income By Province")+theme_light()
```

```{r theme4}
average_incomes %>% 
  #fct_reorder reorders the levels ofthe factor variable by the values of the second numeric variable
  ggplot(., aes(y=fct_reorder(Province, Average_Income), x=Average_Income))+geom_col()+labs(x="Average Household Income", y="Province", title="Average Total Household Income By Province")+theme_minimal()
```

But there are a ton of themes out there that come pre-built in the [`ggthemes`](https://yutannihilation.github.io/allYourFigureAreBelongToUs/ggthemes/) package.

```{r theme5}
#instal if necessary
#install.packages('ggthemes')
library(ggthemes)
```

```{r theme6}
average_incomes %>% 
  #fct_reorder reorders the levels ofthe factor variable by the values of the second numeric variable
  ggplot(., aes(y=fct_reorder(Province, Average_Income), x=Average_Income))+geom_col()+labs(x="Average Household Income", y="Province", title="Average Total Household Income By Province")+theme_economist()
```

And the Wall Street Journal....

```{r theme7}
average_incomes %>% 
  #fct_reorder reorders the levels ofthe factor variable by the values of the second numeric variable
  ggplot(., aes(y=fct_reorder(Province, Average_Income), x=Average_Income))+geom_col()+labs(x="Average Household Income", y="Province", title="Average Total Household Income By Province")+theme_wsj()
```

<!-- And the Globe and Mail!!! Althought, perhaps not surprisingly, this does not work as well.  -->


<!-- ```{r tgam} -->
<!-- #install if necessary from github -->
<!-- devtools::install_github("globeandmail/tgamtheme") -->
<!-- library(tgamtheme) -->
<!-- average_incomes %>%  -->
<!--   #fct_reorder reorders the levels ofthe factor variable by the values of the second numeric variable -->
<!--   ggplot(., aes(y=fct_reorder(Province, Average_Income), x=Average_Income))+geom_col()+labs(x="Average Household Income", y="Province", title="Average Total Household Income By Province")+scale_fill_tgam() -->
<!-- ``` -->

## Axis Text Font and Size
We can also modify the size of the text on the axes. 

Here we need to understand the `theme()` layer. `theme()` can accept arguments at a very high level of generality, right down to extremely specific. 

So, for example, we can change the font family, the size and the color of all the text in the graph. 

```{r axis1}
average_incomes %>% 
  #fct_reorder reorders the levels ofthe factor variable by the values of the second numeric variable
  ggplot(., aes(y=fct_reorder(Province, Average_Income), x=Average_Income))+geom_col()+labs(x="Average Household Income", y="Province", title="Average Total Household Income By Province")+theme(text=element_text(size=14, color="red", family="Times New Roman"))

```

But we can get more specific, maybe we only want to set the title to be a different color, like dark green, to fit the bars better. 

```{r axis2}
average_incomes %>% 
  #fct_reorder reorders the levels ofthe factor variable by the values of the second numeric variable
  ggplot(., aes(y=fct_reorder(Province, Average_Income), x=Average_Income))+geom_col()+labs(x="Average Household Income", y="Province", title="Average Total Household Income By Province")+theme(text=element_text(family="Times New Roman"), plot.title=element_text(color="darkgreen"))

```
Notice though that the axis tick labels are untouched. Here we can change those by modifying `axis.text`. Let's make them smaller. 


```{r axis3}
average_incomes %>% 
  #fct_reorder reorders the levels ofthe factor variable by the values of the second numeric variable
  ggplot(., aes(y=fct_reorder(Province, Average_Income), x=Average_Income))+geom_col()+labs(x="Average Household Income", y="Province", title="Average Total Household Income By Province")+theme(text=element_text(family="Times New Roman"), plot.title=element_text(color="darkgreen"), axis.text=element_text(size=5))
```

But we can also only modify the size of each axis separately. 
```{r axis4}
average_incomes %>% 
  #fct_reorder reorders the levels ofthe factor variable by the values of the second numeric variable
  ggplot(., aes(y=fct_reorder(Province, Average_Income), x=Average_Income))+geom_col()+labs(x="Average Household Income", y="Province", title="Average Total Household Income By Province")+theme(text=element_text(family="Times New Roman"), plot.title=element_text(color="darkgreen"), axis.text=element_text(size=5))

```

We can also rotate the angle of the axis text labels and change the color.


```{r axis5}
average_incomes %>% 
  #fct_reorder reorders the levels ofthe factor variable by the values of the second numeric variable
  ggplot(., aes(y=fct_reorder(Province, Average_Income), x=Average_Income))+geom_col()+labs(x="Average Household Income", y="Province", title="Average Total Household Income By Province")+theme(text=element_text(family="Times New Roman"), plot.title=element_text(color="darkgreen"), axis.text.y=element_text(size=5), axis.text.x=element_text(size=12, angle=45))

```

We can also use `theme()` to modify the colors of the backgrounds and the size of the grid lines. Sometimes we want them, sometimes we don't. 

This version of our graph was looking the best, I think.  but let's set the background to be blank. 



```{r background1}
average_incomes %>% 
  #fct_reorder reorders the levels ofthe factor variable by the values of the second numeric variable
  ggplot(., aes(y=fct_reorder(Province, Average_Income), x=Average_Income))+geom_col()+labs(x="Average Household Income", y="Province", title="Average Total Household Income By Province")+theme(plot.title=element_text(color="darkgreen"), axis.text.y=element_text(size=5),panel.background=element_blank())
```

But we may want to add some thicker grid lines. 

```{r background2}
average_incomes %>% 
  #fct_reorder reorders the levels ofthe factor variable by the values of the second numeric variable
  ggplot(., aes(y=fct_reorder(Province, Average_Income), x=Average_Income))+geom_col()+labs(x="Average Household Income", y="Province", title="Average Total Household Income By Province")+theme(plot.title=element_text(color="darkgreen"), axis.text.y=element_text(size=5),panel.background=element_rect(fill='white'), panel.grid.major = element_line(colour="black"))
```
That doesn't look so good, really we only want the x-axis lines to facilitate people being able to estimate the number.  Plust, instead of filling the panel background with white, let's just delete it altogether. To do that, we can change `panel.background` to `element_blank()`. This let's us set the panel grid lines on top. 
```{r background3}
average_incomes %>% 
  #fct_reorder reorders the levels ofthe factor variable by the values of the second numeric variable
  ggplot(., aes(y=fct_reorder(Province, Average_Income), x=Average_Income))+geom_col()+labs(x="Average Household Income", y="Province", title="Average Total Household Income By Province")+theme(plot.title=element_text(color="darkgreen"), axis.text.y=element_text(size=5),panel.background=element_blank(), panel.grid.major.x = element_line(colour="darkgray", linetype=2, size=0.5), panel.ontop = T)
```

## Legend 

Sometimes we want to move the legend around. To move it to the bottom, we add `legend.position="bottom"` to the `theme()` function

```{r background4}
average_incomes %>% 
  #fct_reorder reorders the levels ofthe factor variable by the values of the second numeric variable
  ggplot(., aes(y=fct_reorder(Province, Average_Income), x=Average_Income))+geom_col()+labs(x="Average Household Income", y="Province", title="Average Total Household Income By Province")+theme(plot.title=element_text(color="darkgreen"), axis.text.y=element_text(size=5),panel.background=element_blank(), panel.grid.major.x = element_line(colour="darkgray", linetype=2, size=0.5), panel.ontop = T, legend.position="bottom")
```



## Customizing Themes

You can modify all these parts graph by graph, or you can go further and actually customize your theme.  

Let's start by storing the regular theme 


```{r custom-theme1}
theme_mine<-function() {
  theme(
  panel.background=element_rect(fill="darkred")
)}
```

Let's check to see if that works:

```{r custom-theme2}
average_incomes %>% 
  #fct_reorder reorders the levels ofthe factor variable by the values of the second numeric variable
  ggplot(., aes(y=fct_reorder(Province, Average_Income), x=Average_Income))+geom_col()+labs(x="Average Household Income", y="Province", title="Average Total Household Income By Province")+theme_mine()

```

Let's pick a font family:


```{r custom-theme3}
theme_mine<-function() {
  theme(
    text=element_text(family="mono"),
  panel.background=element_blank(),
  panel.ontop = T,
  panel.grid.major.x = element_line(color="lightgray", linetype=2),
  plot.title=element_text(color="darkgreen")
  )
}

```

And let's redraw the plot again:

```{r custom-theme4}
average_incomes %>% 
  #fct_reorder reorders the levels ofthe factor variable by the values of the second numeric variable
  ggplot(., aes(y=fct_reorder(Province, Average_Income), x=Average_Income))+geom_col()+labs(x="Average Household Income", y="Province", title="Average Total Household Income By Province")+theme_mine()
```



<!-- # Interactvitiy -->
<!-- It is often fun to add (and useful to add interactivity to graphs. In this case where there is a lot of information, we might want to highlight or expand). To do this we can use the [`plotly`](https://plotly-r.com/index.html) library.  -->

<!-- To make it work, we need to save our graph in an object, here, `myplot`. -->

<!-- ```{r plotly1} -->
<!-- #Install if necessary  -->
<!-- #install.packages('plotly') -->
<!-- library(plotly) -->
<!-- myplot<-shs_subset %>%  -->
<!--   group_by(`Board`, Province) %>%  -->
<!--   summarize(average=mean(Income, na.rm=T)) %>%  -->
<!--   filter(!is.na(average)) %>%  -->
<!--     ggplot(., aes(y=fct_reorder(Province, average, .desc=T), x=average, fill=`Board`, text=Province))+geom_col()+scale_fill_brewer(type="qualitative", palette="Greens")+geom_col()+labs(x="Average", y="School Board", title="Percent of Students Meeting Academic Math Standards in Grade 9")+theme_mine() -->


<!-- ``` -->

<!-- Then we just wrap the plot in the command `ggplotly` -->

<!-- ```{r plotly2} -->
<!-- ggplotly(myplot) -->
<!-- ``` -->

<!-- By default the `tooltip` displays all of the aesthetics that have been used to make the graph, here the `x`, `y`, and `fill` aesthetics.  -->

<!-- But notice that we have this ugly `fct_reorder` bit for the board name. That's because that's how we defined the `y` variable in the `ggplot` command. we can customize this pretty easily. -->
<!-- First, let's add a `label` aesthetic using the original Province variable.  -->

<!-- Then, we can suppress the `y` aesthetic. And while we're at it, let's go through and change `average` to `Average` to make it look nicer.  -->

<!-- ```{r plotly3} -->

<!-- myplot<-shs_subset %>%  -->
<!--   group_by(`Board`, Province) %>%  -->
<!--   summarize(Average=mean(Income, na.rm=T)) %>%  -->
<!--   filter(!is.na(Average)) %>%  -->
<!--     ggplot(., aes(y=fct_reorder(Province, Average, .desc=T), x=Average, fill=`Board`, label=Province))+geom_col()+scale_fill_brewer(type="qualitative", palette="Greens")+geom_col()+labs(x="Average", y="School Board", title="Percent of Students Meeting Academic Math Standards in Grade 9")+theme_mine() -->
<!-- ggplotly(myplot, tooltip=c("label", "x")) -->

<!-- ``` -->

<!-- Now we could actually suppress the confusing and hard to read board names on the y-axis.  -->


<!-- ```{r plotly4} -->

<!-- myplot<-shs_subset %>%  -->
<!--   group_by(`Board`, Province) %>%  -->
<!--   summarize(Average=mean(Income, na.rm=T)) %>%  -->
<!--   filter(!is.na(Average)) %>%  -->
<!--     ggplot(., aes(y=fct_reorder(Province, Average, .desc=T), x=Average, fill=`Board`, label=Province))+geom_col()+scale_fill_brewer(type="qualitative", palette="Greens")+geom_col()+labs(x="Average", y="School Board", title="Percent of Students Meeting Academic Math Standards in Grade 9")+theme_mine()+theme(axis.text.y=element_blank(), axis.ticks.y=element_blank()) -->
<!-- ggplotly(myplot, tooltip=c("label", "x")) -->

<!-- ``` -->


